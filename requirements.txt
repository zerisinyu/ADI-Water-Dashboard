streamlit==1.50.0
pandas==2.2.3
numpy==1.26.4
plotly==6.3.1
google-generativeai==0.8.5
python-dotenv==1.0.1
toml==0.10.2
tomli
tomlkit==0.13.3
openpyxl==3.0.10
# OpenAI-compatible SDK — used for GLM / Grok / OpenAI / DeepSeek / OpenRouter
# (BYOK, MajiBot). Kept as a range, not a hard pin: the exact 1.59.9 pin failed
# to co-install on the hosted deployment, leaving the SDK missing at runtime.
openai>=1.55,<2

# Data Engineering
duckdb==1.1.3
pandera>=0.20.0

# Forecasting & ML
# Forecasting uses statsmodels Holt-Winters (see analytics/forecasting.py).
# statsforecast was removed: it pulled in numba + llvmlite, which are heavy
# and slow to install on Streamlit Cloud.
statsmodels>=0.14.0
scikit-learn>=1.5.0

# Report Export
fpdf2>=2.8.0